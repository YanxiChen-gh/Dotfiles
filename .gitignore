.env.local
.env.*.local

# agent-maturity: raw transcripts pulled from Ona envs (bulky, may contain code)
claude/agent-maturity/evidence/
# agent-maturity: personal/internal DATA - must NOT go to this PUBLIC repo
# (interventions reference internal PRs/tickets/tools). Keep in a private store.
claude/agent-maturity/interventions.jsonl
claude/agent-maturity/tracker.md

# doc/PR style-harness corpora + eval results reference internal Vanta content
# (real PR bodies, code diffs, review comments, docs). Keep local only - PUBLIC repo.
claude/doc-style/eval/corpus/
claude/pr-style/eval/corpus/
claude/doc-style/eval/results-*.json
claude/pr-style/eval/results-*.json

# Python bytecode
__pycache__/
*.pyc

# Lavish review artifacts (ephemeral)
.lavish/
